name: internal_application
description: アプリケーション層のロジックを提供し、ユースケースやサービスの調整を担うパッケージです。
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  internal_domain_logic:
    path: ../domain_logic
  internal_domain_model:
    path: ../domain_model
  pub_semver: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
  test: ^1.25.15

flutter:
  uses-material-design: true

name: cores_model
description: This package provides model classes.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  freezed_annotation: ^3.0.0

dev_dependencies:
  build_runner: ^2.4.15
  freezed: ^3.0.0
  test: ^1.25.8

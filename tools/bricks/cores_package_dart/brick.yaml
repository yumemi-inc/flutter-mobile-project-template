name: cores_package_dart
description: A new brick created with the Mason CLI.

version: 0.1.0+1

environment:
  mason: ">=0.1.1 <0.1.2"

vars:
  package_name:
    type: string
    description: Package name
    prompt: What is Package name?

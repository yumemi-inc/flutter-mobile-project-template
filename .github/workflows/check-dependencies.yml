name: Check dependencies

on:
  pull_request:
    types: [opened, synchronize]
    paths:
      - '**/pubspec.yaml'
      - '**/pubspec.lock'

  # TODO: 検証用に追加したため、merge前に削除する
  push:
    branches:
      - feature/refactor-pkg-depend
  workflow_dispatch:

env:
  MD_NAME: diff.md

jobs:
  check-dependencies:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    outputs:
      MD_PATH: ${{ steps.dependencies.outputs.MD_PATH }}
    steps:
      # https://github.com/marketplace/actions/checkout
      - name: Checkout
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332 # v4.1.7

      - name: Setup Application Runtime
        uses: ./.github/actions/setup-application-runtime

      - name: Check dependent packages
        id: dependencies
        run: |
          branch=$(git branch --show-current)

          echo "Run bracnh is $branch"
          
          baseRefName=$(gh pr view $branch --json baseRefName --jq .baseRefName)

          echo "The base branch is $baseRefName"

          diff=$(dart run ./tools/diff_yaml.dart $baseRefName pubspec.yaml pubspec.lock)

          if [ -z "$diff" ]; then
            echo "No change in pubspeck.lock"
          else
            echo "There is a change in pubspeck.lock."
            echo $diff > $MD_NAME
            path="$GITHUB_WORKSPACE/$FILE_NAME"
            echo "MD_PATH=$path" >> $GITHUB_OUTPUT
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  markdown:
    uses: ./.github/workflows/wc-check-markdown.yaml
    needs: check-dependencies
    if: ${{ needs.check-dependencies.outputs.MD_PATH != '' }}

  update-comment:
    runs-on: ubuntu-22.04
    needs: [check-dependencies, markdown]
    permissions:
      contents: read
      pull-requests: write
    timeout-minutes: 5

    steps:
      # https://github.com/marketplace/actions/checkout
      - name: Checkout
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332 # v4.1.7

      - name: Get PR number
        run: |
          branch=$(git branch --show-current)
          echo 'current branch is $branch'
          number=$(gh pr list --head $branch --state open --json number --jq '.[0].numbe')

          echo "PR Number is $number"
          echo "PR_NUMBER=$number" >> $GITHUB_ENV
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # https://github.com/peter-evans/create-or-update-comment
      - name: Comment
        uses: peter-evans/create-or-update-comment@71345be0265236311c031f5c7866368bd1eff043 # v4.0.0
        with:
          issue-number: env.PR_NUMBER
          body-path: ${{ needs.check-dependencies.outputs.MD_PATH }}
            
